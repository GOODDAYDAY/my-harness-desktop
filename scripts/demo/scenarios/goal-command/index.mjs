// goal-command 场景 bundle:只提供种子(项目 + 会话 + 最近打开)。
// 真实断言步骤在 scripts/demo/goal-command.e2e.mjs(steps 语言无断言原语,
// e2e 脚本复用本场景的 seed.json;record.mjs 跑本场景 = 空剧本,仅初始化环境)。
export default {
  name: "goal-command",
  steps: [],
};
