# Dependencies
node_modules

# Build output（electron-vite 产物）
out/
dist/

# 临时/缓存
.DS_Store
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# 本地环境与密钥
.env
.env.local
.env.*.local

# IDE
.vscode/
.idea/
*.swp
*.swo

# 旧工程残留归档（不属于本项目）
docs.zip

# 运行时本地配置（dev 跑起来的 app 数据，不入库）
.my-harness-desktop/

# dsh 会话根溢出（DSH_SESSION_ROOT 未设时落在 cwd 的 .sessions）
.sessions/

# electron-builder 打包产物
release/

.playwright-mcp/**
.playwright-mcp

# Claude Code 本地个人配置（.claude/skills/ 共享入库，个人配置不入）
.claude/settings*.json

# multi-agent 运行时状态（本地产物，不入库）
.multi-agent/

# worktree 会话目录（本地隔离产物）
.worktrees/
.claude/worktrees/

# 测试覆盖率产物
coverage/

# Windows 系统垃圾
Thumbs.db
desktop.ini
.pi-desktop/

# e2e 验收的临时 HOME(verify-e2e/e2e-inmem 产物,不入库)
.e2e-home/

# 模型/权重文件(绝不入库——防大文件污染 git 历史)
*.gguf
*.safetensors
*.onnx
*.pt
*.pth
*.ckpt
*.h5
*.hdf5
*.tflite
*.mlmodel
*.mgguf

# 模型下载/缓存目录(本地产物;仅根目录,不碰 src/**/models/ 等 TS 源码目录)
/models/
.models/
.cache/huggingface/
.cache/lm-studio/
.ollama/
.lmstudio/
.qa-shots/
